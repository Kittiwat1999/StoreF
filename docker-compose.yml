version: "3.9"
name: "StoreFrontManagement"
services:
  minio:
    image: minio/minio
    container_name: storef-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"   # S3 API
      - "9001:9001"   # Web console (view bucket/files via browser)
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - app_network
  web:
    build:
      context: ./Web
      dockerfile: Dockerfile
    container_name: storef-web
    env_file:
      - ./.env
    volumes:
      - ./Web:/app
      - /app/node_modules
    ports:
      - "${VITE_PORT}:5173"
    command: npm run dev -- --host 0.0.0.0 --port 5173
    networks:
      - app_network

  api:
    build:
      context: ./StoreF
      dockerfile: Dockerfile
      target: dev
    container_name: storef-api
    restart: unless-stopped
    depends_on:
      - db
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Devlopment}
      ConnectionStrings__DefaultConnection: >-
        Host=db;Port=5432;Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD}
      Jwt__Secret: ${SECRET_KEY}
      ASPNETCORE_URLS: http://+:8000
      Minio__Endpoint: minio:9000
      Minio__AccessKey: ${MINIO_ROOT_USER}
      Minio__SecretKey: ${MINIO_ROOT_PASSWORD}
      Minio__BucketName: product-images
    volumes:
      - ./StoreF/src:/src/src
    ports:
      - "8000:8000"
    networks:
      - app_network

  db:
    image: postgres:15
    container_name: storef-postgres
    env_file:
      - ./.env
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - app_network

  pgadmin:
    image: dpage/pgadmin4
    container_name: storef-pgadmin
    env_file:
      - ./.env
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - db
    networks:
      - app_network

  nginx:
    image: nginx:alpine
    container_name: storef-nginx
    restart: unless-stopped
    depends_on:
      - api
      - web
      - minio
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "80:80"
    networks:
      - app_network

volumes:
  postgres_data:
  pgadmin_data:
  minio_data:

networks:
  app_network:
    driver: bridge
